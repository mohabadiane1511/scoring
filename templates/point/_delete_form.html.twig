<form method="post" action="{{ path('app_point_delete', {'id': point.id}) }}" onsubmit="return confirm('Etes vous sur de vouloir supprimer cette ligne ?');">
	<input type="hidden" name="_token" value="{{ csrf_token('delete' ~ point.id) }}">
	<button class="btn btn-danger">Delete</button>
</form>
